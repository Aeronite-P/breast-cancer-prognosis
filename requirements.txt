# --- Core data stack ---
pandas>=2.0
numpy>=1.24
requests>=2.31
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3

# --- Survival modeling + interpretability ---
lifelines>=0.30
scikit-survival>=0.23
shap>=0.44

# --- Whole-slide imaging (Stages C–J) ---
torch>=2.8
torchvision>=0.23
transformers>=4.57        # loads the Phikon pathology foundation model (owkin/phikon)
openslide-python>=1.4
openslide-bin>=4.0        # bundles the OpenSlide C library (no separate install needed)
pillow>=11.0
